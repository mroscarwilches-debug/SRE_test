# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
crash.*.log

# Lambda build artifacts
lambda/*.zip

# Lambda layer build artifacts (generados por scripts/build_layer.sh)
layer/python/
layer/*.zip

# Python
__pycache__/
*.pyc

# Misc
*.tmp

# Claude Code (config local de cada maquina, no se versiona)
.claude/
